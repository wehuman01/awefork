node_modules/
# the worktree setup symlinks node_modules, which git tracks as a plain file
node_modules
out/
dist/
*.log
*.tsbuildinfo
.DS_Store
.vitest/
.superpowers/
# nested git worktrees (local checkouts of other branches); biome's
# useIgnoreFile skips them, so their biome.json can't trip the nested-root check
.worktrees/
/.zcode
